var name = "Angela";
name.toUpperCase();

name = name.toUpperCase();// Change permanently
console.log(name);

var name = "ANGELA";
name.toLowerCase();

name = name.toLowerCase();
console.log(name);